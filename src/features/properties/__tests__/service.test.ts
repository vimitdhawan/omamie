import { describe, it, expect } from "vitest";

describe("Properties Service", () => {
  describe.skip("submitPropertyStep1", () => {
    it("TODO: Update tests for new domain model architecture", async () => {
      // TODO: Rewrite tests for domain model architecture
      expect(true).toBe(true);
    });
  });
});
